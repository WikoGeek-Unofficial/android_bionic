
How to build kernel open source
Steps for building

export PATH=~/Your_Toolchain_PATH/:$PATH
for example 
export PATH=/alps/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.6/bin:$PATH

mkdir out
make O=out $project_defconfig (Ex. make O=out tinno82_we_aosp_kk_defconfig)
make -j24 -k O=out zImage modules

